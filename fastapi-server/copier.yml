# Project metadata

# ...

# copier

_exclude:
  - copier.yml
  - __pycache__

_tasks:
  - move ".env.example" ".env" >nul 2>&1

_message_after_copy: "Command to run: fastapi dev main.py"