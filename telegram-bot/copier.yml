# Project metadata

project_name:
  type: str
  default: "{{ _folder_name }}"

api_client:
  type: str
  choices:
    - telebot (pyTelegramBotAPI)
    - aiogram3

# copier

_exclude:
  - copier.yml
  - __pycache__

_tasks:
  - move ".env.example" ".env" >nul 2>&1